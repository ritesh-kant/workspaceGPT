# Set these in Vercel Dashboard > Settings > Environment Variables

# Atlassian OAuth proxy
ATLASSIAN_CLIENT_ID=your_client_id_here
ATLASSIAN_CLIENT_SECRET=your_client_secret_here

# Note: the Chrome extension share proxy (retrieval + chat) now lives in the
# Cloudflare Worker — see apps/workspacegpt-worker. This Vercel app only handles
# the Confluence OAuth token exchange.
