{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "window-drag",
  "description": "On macOS the shell page draws its own title bar (TitleBarStyle::Overlay), and Tauri's data-tauri-drag-region script moves the window through these two commands. They are the only IPC the loopback page can reach: nothing that reads, writes, or runs anything.",
  "windows": ["main"],
  "remote": { "urls": ["http://127.0.0.1:*"] },
  "permissions": ["core:window:allow-start-dragging", "core:window:allow-internal-toggle-maximize"]
}
