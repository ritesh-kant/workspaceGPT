{
  "name": "desktop",
  "version": "0.0.1",
  "private": true,
  "description": "WorkspaceGPT Desktop — Tauri shell + Node sidecar running the VS Code extension host through a vscode compat module",
  "scripts": {
    "build": "node sidecar/esbuild.config.mjs",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "usage-check": "node sidecar/usage-check.mjs",
    "dev:headless": "node scripts/dev-headless.mjs",
    "dev": "node scripts/dev-tauri.mjs",
    "seed:from-vscode": "node scripts/seed-from-vscode.mjs"
  },
  "dependencies": {
    "@napi-rs/keyring": "^1.1.0",
    "ws": "8.21.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.5",
    "@types/node": "22.13.0",
    "@types/ws": "^8.5.13",
    "esbuild": "^0.20.1",
    "typescript": "5.7.3",
    "workspacegpt-extension": "workspace:*"
  }
}
