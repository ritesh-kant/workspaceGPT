const A = () => {
    return (
        <div>
            {true ? (
                true ? (
                <div />
                ) : (
                <div />
                )
            ) : (
                <div />
            )}
        </div>
    );
};
